// Este archivo se crea con el objetivo de facilitar la exportació:

const Categoria = require('./categoria');
const Producto = require('./productos');
const Roles = require('./roles');
const Users = require('./users');


module.exports = {
    Categoria,
    Roles,
    Users,
    Producto
}